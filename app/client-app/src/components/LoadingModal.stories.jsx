import { LoadingModal } from "./LoadingModal";

export default {
  component: LoadingModal,
};

export const Basic = {
  args: {
    verb: "Reticulating",
    noun: "splines",
    isOpen: true,
  },
};
